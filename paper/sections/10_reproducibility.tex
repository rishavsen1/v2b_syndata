Every number in this paper regenerates from the public repository at revision
\owed{final git SHA (stamped by the collect step after the evidence commit)}:
a single driver (\texttt{tools/repro\_paper.py}) executes calibration, the
fidelity suites (S0--S6 with bootstrap confidence intervals), the
building-load comparison, both TSTR experiments, and the mixture ablation
from committed configurations and fixed seeds, and writes the consolidated
results file the paper cites; two consecutive runs produce byte-identical
results (verified), and the full driver completes in ${\approx}8$ minutes
on 32 cores with warm simulation caches. Dataset determinism is enforced by
regression tests (byte-identical output for fixed scenario and seed). The
reference corpus regenerates from its manifests via the recorded (revision,
configuration, seed) triplets at ${\approx}246$ CPU-hours.
