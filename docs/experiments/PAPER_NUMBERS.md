# PAPER_NUMBERS — consolidated evidence for the KDD paper

> **Auto-generated by `tools/repro_paper.py`. Do not edit by hand.**
> Every number the paper cites must appear here; the paper cites only
> numbers present here. Regenerate: `uv run python tools/repro_paper.py`
> (full, ~overnight-safe) or `--steps collect` (rebuild this file from
> existing artifacts).

- **Git revision:** `a63c73ed2257fbde03766db1630d5ab5f3497f52` (WS-F working tree; re-run `--steps collect` after the WS-F commit to stamp the final SHA)
- **Determinism:** all steps seeded (generation: SHA-keyed node streams; S1 bootstrap: base seed 20260708, per-cell hashed sub-streams, B=1000; TSTR: seed 1234; ablation: deterministic EM). Two consecutive driver runs must reproduce this file bit-for-bit; wall-times below are the recorded measurement from `docs/experiments/repro_runtimes.json` (written once; see driver docstring).
- **No timestamps in this file by design** — the git SHA pins the revision; `docs/CALIBRATION_RESULTS.md` carries its own generation date.

## 1. Calibration corpora (Tab 1, §4.1)

| corpus | sessions | drivers/identifiers | provenance of count |
|---|--:|--:|---|
| ACN-Data (caltech+jpl+office001, 2019–21) | 41,774 | 635 (632 assigned) | regenerated: `sources_summary.csv` (ablation step) |
| ElaadNL 4TU/Utrecht (workplace venue filter) | 55,201 | 1,231 | regenerated: `sources_summary.csv` |
| ElaadNL raw archive (pre venue filter) | 55,379 | 3,409 | `docs/CALIBRATION_NOTES.md` (historical ingest count; not re-run here) |
| EV WATTS public 2026 (workplace+public L2; port-as-proxy identity) | 1,265,017 | 3,356 ports | `configs/populations.yaml` calibration_metadata |
| INL EV Project (labeled FIXTURE, not a corpus) | 65 | 4 | `configs/populations.yaml` calibration_metadata |

Generating command: `uv run python tools/repro_paper.py --steps ablation` (sources_summary) — EV WATTS/INL rows from committed calibration metadata.

## 2. Region assignment & weights (§4.2, Fig 2)

| quantity | value | source |
|---|--:|---|
| ACN user share: rare_consistent | 36.1% (229) | `S0_assignment.csv` |
| ACN user share: rare_inconsistent | 0.6% (4) | `S0_assignment.csv` |
| ACN user share: occasional_consistent | 39.8% (253) | `S0_assignment.csv` |
| ACN user share: regular_charger | 23.0% (146) | `S0_assignment.csv` |
| ACN user share: erratic | 0.0% (0) | `S0_assignment.csv` |
| ACN user share: __unassigned__ | 0.5% (3) | `S0_assignment.csv` |
| unassigned drivers: acn | 0.5% | `S0_assignment.csv` |
| unassigned drivers: acn_caltech | 0.4% | `S0_assignment.csv` |
| unassigned drivers: acn_jpl | 0.5% | `S0_assignment.csv` |
| unassigned drivers: acn_office001 | 0.0% | `S0_assignment.csv` |
| unassigned drivers: elaadnl | 0.0% | `S0_assignment.csv` |
| unassigned drivers: evwatts | 0.4% | `S0_assignment.csv` |
| ACN drivers with φ ≤ 0.3 | 77.0% | `sources_summary.csv` |
| ElaadNL unassigned before re-anchoring (historical, 2026-06-27) | 76% | `docs/PROJECT_TRACKER.md` W8/✔4 (the 0% 'after' is the regenerated S0 row above) |

Generating command: `uv run python tools/validate_calibration.py --seeds 50 --workers 16 --sources acn,acn_caltech,acn_jpl,acn_office001,elaadnl,evwatts --bootstrap 1000`.

## 3. Shipped arrival mixture, largest ACN region (Fig 3, §4.3)

- `rare_consistent` arrival = 0.42·N(8.21, 1.25) + 0.58·N(14.54, 3.52), truncated to [4, 22] h; fit KS 0.040 (n = 4,676).
- Source: `configs/populations.yaml` (`acn_workplace_baseline.region_distributions.rare_consistent.arrival`).

## 4. Behavioral fidelity (Tab 2, §5.2, §8)

| quantity | value | source |
|---|---|---|
| S1 mean \|Δμ\| (all 36 region×variable cells, all 6 sources) | 0.44 h | `S1_marginals.csv` |
| S1 mean \|Δμ\| excl. evwatts (30 cells; pre-WS-F aggregate for continuity) | 0.37 h | `S1_marginals.csv` |
| S1 max KS (worst cell) | 0.239 [0.146, 0.385] (acn_office001 / rare_consistent / arrival_hour, n=41) | `S1_marginals.csv` |
| S1 arrival KS, largest ACN region (acn/rare_consistent) | 0.222 [0.209, 0.235] | `S1_marginals.csv` |
| S2 max copula ρ-gap | 0.226 (acn_office001 / occasional_consistent) | `S2_joint.csv` |
| S3 median Δ(holdout − train KS) | 0.064 (36 cells) | `S3_holdout.csv` |
| S3 median Δ excl. evwatts (26 cells; pre-WS-F scope for continuity) | 0.069 | `S3_holdout.csv` |
| S3 worst cell Δ | +0.425 (acn_office001 / regular_charger / arrival_hour, n_test=62) | `S3_holdout.csv` |
| S3 worst dwell cells (top 3 Δ) | +0.290 (acn_caltech/rare_consistent, n_test=384), +0.281 (acn_caltech/occasional_consistent, n_test=363), +0.194 (acn/rare_consistent, n_test=589) | `S3_holdout.csv` |
| S3 worst arrival cells (top 3 Δ) | +0.425 (acn_office001/regular_charger, n_test=62), +0.372 (acn_jpl/regular_charger, n_test=1386), +0.241 (evwatts/erratic, n_test=595) | `S3_holdout.csv` |

Full per-cell table with CIs: `docs/experiments/s1_fidelity_cis.csv` and `docs/CALIBRATION_RESULTS.md`. Generating command as §2.

## 5. Mixture & per-region ablation (§4.3, §5.2)

_Metric: one-sample KS of the fitted CDF vs the region's source sessions (= the fitter's `ks_fit_quality`). Supersedes the planning-doc '0.148→0.073', which had no committed primary source._

| quantity | value | source |
|---|---|---|
| ACN mean arrival KS: single family → shipped (mixture where selected) | 0.100 → 0.042 (3 regions; mixture selected in 3) | `mixture_ablation.csv` |
| arrival cells where the mixture gate fired (all ablation sources) | 14/14; mean KS 0.134 → 0.035 | `mixture_ablation.csv` |
| pooled-broadcast → per-region fit, acn/rare_consistent arrival | 0.196 → 0.040 | `mixture_ablation.csv` |
| dwell: mean KS single → shipped (all ablation sources) | 0.127 → 0.102 (mixture selected in 4/16) | `mixture_ablation.csv` |

Generating command: `uv run python tools/repro_paper.py --steps ablation`. Per-source aggregates: `docs/experiments/mixture_ablation.md`.

## 6. Building-load fidelity vs ComStock (Tab 3, §5.1, §8)

| archetype/size | gen kW | ComStock kW | CV(RMSE) % | NMBE % | shape corr (wd) | shape corr (we) | peak-hr Δ | gen LF | ref LF |
|---|--:|--:|--:|--:|--:|--:|--:|--:|--:|
| office/small | 4.1 | 8.0 | 56.7 | 49.1 | 0.708 | 0.271 | 1 | 0.358 | 0.434 |
| office/med | 43.3 | 68.7 | 47.2 | 37.0 | 0.908 | 0.068 | 2 | 0.263 | 0.429 |
| office/large | 773.1 | 608.3 | 43.7 | -27.1 | 0.942 | 0.559 | 3 | 0.428 | 0.469 |
| retail/med | 27.9 | 60.6 | 59.3 | 54.0 | 0.912 | 0.768 | 1 | 0.329 | 0.434 |
| retail/large | 21.5 | 36.6 | 51.6 | 41.2 | 0.866 | 0.772 | 1 | 0.292 | 0.386 |

- Weekday shape correlation range: 0.71–0.94; max peak-hour error 3 h.
- NMBE: 4 archetypes below stock average by 37.0–54.0% (positive NMBE = generator under-predicts); 1 archetype(s) above by 27.1%.
- BDG2 context: 19 real meters (14 office + 5 retail) in `data/buildingload_reference/bdg2_timeseries.parquet` (fetched by `tools/fetch_buildingload_reference.py`).

Source: `data/buildingload_reference/validation_metrics.json`. Generating command: `uv run python tools/validate_buildingload.py -v`.

## 7. PV validation vs NREL PVWatts v8 / PySAM (§5.3)

| quantity | value | source |
|---|---|---|
| annual energy error (primary, standard loss semantics) | +1.27% (ours 164,468 kWh vs 162,403 kWh) | `pv_validation.md` |
| max abs monthly error | 2.53% | `pv_validation.md` |
| hourly CV(RMSE) / NMBE / Pearson r | 15.08% / +1.27% / 0.9939 | `pv_validation.md` |
| derate-equalized (pure physics) annual error | -2.80% | `pv_validation.md` |

Generating command: `uv run python tools/validate_pv.py` (requires PySAM; not re-run by this driver — the committed memo is the primary).

## 8. TSTR / TRTR utility (Tab 4, §6, abstract)

| corpus | regime | MAE ratio | RMSE ratio | source |
|---|---|--:|--:|---|
| ACN-Data | lagged, raw | 0.99 | 0.92 | `data/tstr/results.json` |
| ACN-Data | calendar-only, raw | 0.57 | 0.79 | `data/tstr/results.json` |
| ElaadNL | lagged, raw | 7.38 | 6.16 | `data/tstr/results_elaadnl_matched.json` |
| ElaadNL | lagged, normalized | 4.19 | 3.72 | `data/tstr/results_elaadnl_matched.json` |
| ElaadNL | calendar-only, raw | 1.32 | 1.31 | `data/tstr/results_elaadnl_matched.json` |
| ElaadNL | calendar-only, normalized | 1.02 | 1.03 | `data/tstr/results_elaadnl_matched.json` |

| context quantity | value | source |
|---|---|---|
| ACN synthetic cohort | scenario S_acn_caltech, seed 1234, 90 sessions | `data/tstr/results.json` |
| ElaadNL synthetic cohort | scenario S_elaadnl_public_eu, seed 1234, 75 sessions, mean 2.16 kW, peak 26.0 kW | `data/tstr/results_elaadnl_matched.json` |
| ElaadNL real series | mean 33.15 kW, peak 481.1 kW, 35,980 hourly bins | `data/tstr/results_elaadnl_matched.json` |
| ElaadNL real cohort identifiers (workplace venue filter) | 1,231 drivers (raw archive: 3,409 identifiers, `docs/CALIBRATION_NOTES.md`) | `sources_summary.csv` |
| superseded artifact (mismatched-scenario ElaadNL run, kept as evidence per §6 footnote) | `data/tstr/results_elaadnl.json` (scenario S_acn_caltech) | repo |

Generating commands: `uv run python tools/tstr_forecasting.py --real acn --out data/tstr/results.json` and `uv run python tools/tstr_forecasting.py --real elaadnl --normalize --out data/tstr/results_elaadnl_matched.json`.

## 9. Release inventory, corpus, compute (§3, §7, §10, abstract)

| quantity | value | source / command |
|---|---|---|
| benchmark scenario configurations | 60 | `ls configs/scenarios/*.yaml \| wc -l` |
| typed knobs in the registry | 69 | `configs/knobs.yaml` (leaf specs with a `type`) |
| validation invariants | 59 in 9 families (A–I) | distinct `<letter><n>:` codes in `src/v2b_syndata/validate.py` |
| reference corpus | 10 buildings × 12 months × 150 samples = 18,000 units | `data/output/campus10/*/batch_manifest.json` |
| corpus generation outcome | 18,000 succeeded / 0 failed; 0 hard validation errors; 11,920 units carry soft advisories | batch manifests (validation summaries) |
| corpus size on disk | 18.2 GiB | `du -sb data/output/campus10` |
| per-unit generation cost | 49.2 s (mean over 18,000 units, 1 core each) | batch manifests (`duration_sec`) |
| corpus compute | 246 CPU-hours (30 workers ≈ 8.2 h wall) | batch manifests |

## 10. Reproduction compute (this driver, recorded measurement)

| step | wall time |
|---|--:|
| calibration | 7.6 min |
| buildingload | 0.5 min |
| tstr_acn | 0.1 min |
| tstr_elaadnl | 0.3 min |
| ablation | 0.4 min |
| collect | 0.0 min |
| **total** | **8.8 min** |

_Measured once on a 32-core workstation (EnergyPlus 24.1, load caches warm) and recorded in `docs/experiments/repro_runtimes.json`; re-runs embed the recorded values so this file stays bit-reproducible. Delete that JSON or pass `--record-runtimes` to re-measure._

