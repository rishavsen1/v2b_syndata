# S_dr_bip.yaml — SMUD BIP closure scenario
#
# Sacramento winter window. BIP runs year-round so winter events are
# allowed but rare (low temp_factor).

scenario_id: S_dr_bip
description: "Sacramento winter — SMUD BIP, medium office"

descriptors:
  location:   sacramento_ca
  building:   medium_office_v1
  population: consent_default
  equipment:  balanced_50pct
  noise:      clean

overrides:
  sim_window.mode: custom
  sim_window.start: '2020-01-01'
  sim_window.custom_end: '2020-04-01'
  utility_rate.dr_lambda_base: 0.1
